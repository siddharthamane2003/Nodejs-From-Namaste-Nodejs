1. - Intallatin visite  the node.js website and install usign commands and just sofwtare.
   - verify installtion by varsion of node and node.

2. Now we have to write node code in ternimal as "REPL" MODE -> READ EVALUATE PRINT LOOP 
  -Just type node to active node in cmd and write node code or js code

3. Node.js -> Js Runtime Env and Behind scens v8 engine.
            - v8 engine excute the js code
            -  run the code in vs file as node file.js
// INT
4. - In node.js global Object is => global it have super as the setimeout , cleartimeout.
   - "these" will not point to global object. {} empty Object.
  
5. In old time every runtime env have separte global object
   so then deicded to globalThis it will applicble to evry runtime env.

globalThis it will point to global object
 globalThis  is eqaul  global.