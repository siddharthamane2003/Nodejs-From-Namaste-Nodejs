1. MongoDB - We can install sofwtare or the thier are mongodb website that we can upload it datbase it will mange it. - We use mongoDB website we use in that 2 types as the free and paid.

2. - Go to MongoDb web site .
   - Craete free cluster.
   - create user.
   - get the connection string from we can connect our databse to mongodb.

3. We have to see datbase , collections for that we have intall compass.

- compass it will we have to use for the CRUD OPRATION ETC WE HAVE TO SEE DATABASE.

4. - we create database and add the document/collection inform like key value pair.
   - if we do not give id then aumoaticly mogdo will give

- mongodb -> It manages databases
- MongoDB Compass is a GUI tool (desktop app). View databases and collections. Perform CRUD operations on documents.
- Node.js is used to write the backend logic.

5. connect node to mongodb visit mongodb website.

1. MongoDB wesbite create account. In that create the cluster and connect. while connecting stroing password/
2. install mongoDB campus. and connect using share link. and in that write password <>.
3. install mongobd in vs code. using commadn npm mongodb.
4. connect node to monogodb in that visite websit as https://mongodb.github.io/node-mongodb-native/6.8/ and copy code and simply paste in vs code. chage the url and must password will write. and then run.

5. read the collection/doc from the mongodb. 
make sure write dabase name and collectin name.
visist and  copy past code of the read doc from the mongoDB.

6. Insert Data to MongoDB .
CRDU OPRATION.
7. GO THE COLLECTION PAGE IN WEBSITE THE USE METHODS.

8. count , find.