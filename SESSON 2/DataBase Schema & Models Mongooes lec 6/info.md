Monogooes installtion for the datbase connection.
https://www.npmjs.com/package/mongoose more info

1. Connect cluter in mongodb compass-> for that we have to install monogooes ->npm install mongoose
connect to cluter not datbase

2. we have to import the datbase.js file to app.js file
we have to connect cluseter to with asyc fn.
 then  we have to craete the DevTinder DataBase in cluster

But the issues is sever is listing the api and then it will connect DatBase.
sol: 1st we have to connect Dabase and then start srver to list the api.

3. Schema : what are the infomartion are stored in collection as the name , email that are decided of schema.
Model is Crate the Instances.

4. Add the Data into Databse. useing post http methods we are post data. 
- api as the Post/sigup.
- create the dummy object in that data.
- import model and craete instace.
- aplly save to save into datbase.
- handke usinf the try catch block

Note : Scehma and Dummy Definations are must be mathced